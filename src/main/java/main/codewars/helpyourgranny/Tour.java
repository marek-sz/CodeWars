package main.codewars.helpyourgranny;

import java.util.Map;

public class Tour {

    public static int tour(String[] arrFriends, String[][] ftwns, Map<String, Double> h) {

        // your code
        return 1;
    }

    public static int calculateSecondCatheus(double firstCatheus, double hypotenuse) {
        return (int) Math.sqrt(Math.pow(hypotenuse, 2) - Math.pow(firstCatheus, 2));
    }
}
